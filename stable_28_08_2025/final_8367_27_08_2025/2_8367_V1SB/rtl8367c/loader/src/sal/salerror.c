#pragma STRING(far)
#include <salerror.h>

#if 0
const char *
sal_err_msg_get(int32 errCode)
{
    switch (errCode)
    {
	    default:
		    return "Failed to get port isolation";
    }    
}
#endif

